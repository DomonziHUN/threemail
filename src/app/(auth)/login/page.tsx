import { LoginPageContent } from "@/components/auth/login-page-content";

export const dynamic = 'force-dynamic';

export const metadata = {
  title: "Login | Threemail",
};

export default function LoginPage() {
  return <LoginPageContent />;
}
