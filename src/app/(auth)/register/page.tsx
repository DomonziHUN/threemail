import { RegisterPageContent } from "@/components/auth/register-page-content";

export const metadata = {
  title: "Register | ThreeMail Bank",
};

export default function RegisterPage() {
  return <RegisterPageContent />;
}
